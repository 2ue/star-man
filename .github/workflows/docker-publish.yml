name: Docker 镜像发布

# 当推送 tag (格式: v*.*.*)  时触发
on:
  push:
    tags:
      - 'v*.*.*'

env:
  # DockerHub 用户名
  DOCKERHUB_USERNAME: huby11111
  # 镜像基础名称（仓库名）
  REPO_NAME: ${{ github.event.repository.name }}
  # 注意: pnpm 版本在 Dockerfile 中指定 (pnpm@7.33.7)
  # 所有构建操作都在 Docker 容器内执行，因此无需在 Actions 环境中设置 pnpm

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 需要 write 权限以创建 GitHub Release
      packages: write  # 需要此权限以推送到 GitHub Container Registry

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 登录 Docker Hub
      - name: 登录 Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 登录 GitHub Container Registry
      - name: 登录 GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 提取 Docker 元数据（标签、label等）
      # 同时为两个 registry 生成 tags
      # Docker Hub 使用 DOCKERHUB_USERNAME，GHCR 使用 GitHub repository
      - name: 提取 Docker 元数据
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            docker.io/${{ env.DOCKERHUB_USERNAME }}/${{ env.REPO_NAME }}
            ghcr.io/${{ github.repository }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest,enable={{is_default_branch}}

      # 构建并推送 Docker 镜像（并行推送到两个 registry）
      - name: 构建并推送 Docker 镜像
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64,linux/arm64

      # 更新 Docker Hub README
      - name: 更新 Docker Hub README
        uses: peter-evans/dockerhub-description@v4
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: ${{ env.DOCKERHUB_USERNAME }}/${{ env.REPO_NAME }}
          readme-filepath: ./README.md

      # 创建 GitHub Release
      - name: 创建 GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          body: |
            ## 🐳 Docker 镜像

            ### 镜像仓库
            - **Docker Hub:** [`docker.io/${{ env.DOCKERHUB_USERNAME }}/${{ env.REPO_NAME }}`](https://hub.docker.com/r/${{ env.DOCKERHUB_USERNAME }}/${{ env.REPO_NAME }})
            - **GitHub Container Registry:** [`ghcr.io/${{ github.repository }}`](https://github.com/${{ github.repository }}/pkgs/container/${{ env.REPO_NAME }})

            ### 使用方法
            ```bash
            # 从 Docker Hub 拉取
            docker pull docker.io/${{ env.DOCKERHUB_USERNAME }}/${{ env.REPO_NAME }}:${{ github.ref_name }}

            # 从 GitHub Container Registry 拉取
            docker pull ghcr.io/${{ github.repository }}:${{ github.ref_name }}
            ```

            ### 生成的标签
            ```
            ${{ steps.meta.outputs.tags }}
            ```

      # 生成发布摘要
      - name: 生成摘要
        run: |
          echo "## Docker 镜像发布成功 🚀" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 已发布到以下镜像仓库：" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "1. **Docker Hub:** [\`docker.io/${{ env.DOCKERHUB_USERNAME }}/${{ env.REPO_NAME }}\`](https://hub.docker.com/r/${{ env.DOCKERHUB_USERNAME }}/${{ env.REPO_NAME }})" >> $GITHUB_STEP_SUMMARY
          echo "2. **GitHub Container Registry:** [\`ghcr.io/${{ github.repository }}\`](https://github.com/${{ github.repository }}/pkgs/container/${{ env.REPO_NAME }})" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### GitHub Release：" >> $GITHUB_STEP_SUMMARY
          echo "[${{ github.ref_name }}](https://github.com/${{ github.repository }}/releases/tag/${{ github.ref_name }})" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 生成的标签：" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 使用方法：" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "# 从 Docker Hub 拉取" >> $GITHUB_STEP_SUMMARY
          echo "docker pull docker.io/${{ env.DOCKERHUB_USERNAME }}/${{ env.REPO_NAME }}:${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "# 从 GitHub Container Registry 拉取" >> $GITHUB_STEP_SUMMARY
          echo "docker pull ghcr.io/${{ github.repository }}:${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
