# ================================
# Web 应用环境配置
# ================================

# --------------------------------
# 开发服务器配置
# --------------------------------
# 开发服务器端口 (自定义端口，避免与其他服务冲突)
VITE_DEV_PORT=3800

# 开发服务器主机 (默认: true，表示监听所有网卡)
# 设置为 false 只监听 localhost
VITE_DEV_HOST=true

# --------------------------------
# API 配置
# --------------------------------
# API 代理目标地址（开发环境必需）
# 开发环境下 Vite 会将 /api 请求代理到此地址
VITE_API_PROXY_TARGET=http://localhost:3801

# 生产环境 API 基础地址（可选）
# 如果不设置，生产环境下将使用相对路径
# VITE_API_URL=https://api.example.com

# API 请求默认超时时间（毫秒，默认: 10000）
VITE_API_TIMEOUT=10000

# 同步操作超时时间（毫秒，默认: 180000，即 3 分钟）
VITE_SYNC_TIMEOUT=180000

# --------------------------------
# 应用配置
# --------------------------------
# 统计数据刷新间隔（毫秒，默认: 30000，即 30 秒）
VITE_STATS_REFRESH_INTERVAL=30000

# --------------------------------
# 调试配置
# --------------------------------
# 是否启用 API 请求日志（true/false，默认: false）
# 仅在开发环境下生效
VITE_ENABLE_API_LOGGING=false

# 是否启用代理日志（true/false，默认: false）
# 仅在开发环境下生效
VITE_ENABLE_PROXY_LOGGING=false